{{ header }}
</section>

<link href="catalog/view/theme/default/stylesheet/dostavimstyle.css" rel="stylesheet">

<main class="main-page container-fluid">

   <section class="catalog">
      <div class="catalog__container">

         {% if error_warning %}
         <div class="alert alert-danger">{{ error_warning }}</div>
         {% endif %}

         <h1 class="title">{{ heading_title }}</h1>

